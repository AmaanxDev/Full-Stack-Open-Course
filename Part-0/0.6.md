sequenceDiagram
    participant browser
    participant server

    Note right of browser: User clicks Save. JS updates the UI immediately.

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: {"message": "note created"} (Status 201)
    deactivate server

    Note right of browser: Browser stays on the same page. No reload.